services:
  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - 27017:27017

  pos:
    image: pos:latest
    container_name: pos
    ports:
      - 8080:8080
    depends_on:
      - mongo
